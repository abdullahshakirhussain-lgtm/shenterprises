node_modules
.next
out
build
.env
.env.local
prisma/dev.db
prisma/dev.db-journal
# Keep bank deposit slips local — these are sensitive customer data
public/uploads/slips/*
!public/uploads/slips/.gitkeep
# Product images are tracked so they ship with the deploy
!public/uploads/products/
